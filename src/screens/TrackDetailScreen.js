import React from "react";
import {View, Text, StyleSheet} from 'react-native';

const TrackDetailScreen = () => {
    return (
        <Text style={{fontSize: 30}}>TrackDetail Screen</Text>
    );
};

const styles = StyleSheet.create({
    
});

export default TrackDetailScreen;